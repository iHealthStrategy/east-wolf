package com.ihealth.Printer;

public enum BluetoothPrinterStatus {
        OPEN,
        CLOSED,
        CONNECTING,
        CONNECTED,
        DISCONNECTED,
        SEARCHING,
        SEARCHING_STOPPED,
        SEARCHING_CANCELED,
        CONNECT_FAIL
        }
